# MESSAGES LEDGER

Optional cold shortcut to exact message wording. It may remain empty. A message does not require a ledger row merely because it occurred.

Pointer: safe POSIX source path relative to ARCHIVE plus its literal stable heading, for example `sessions/s01/01_exchange.md#M-save01-01`. Existing legacy MESSAGES.md pointers remain valid. Store a routing gist, not the exchange. Keep outer pipes and a contiguous row block; use `unknown` for genuinely missing values.

| when | from | to | gist | pointer |
|---|---|---|---|---|
