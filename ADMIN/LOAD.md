# LOAD — bind an accepted campaign

Use for an explicit request to load an existing module into a clean unbound kit. NEW GAME uses the shared bind steps with its already reviewed draft. Neither entry resets an existing run.

## Check and propose

1. Resolve `RECOVERY/ACTIVE.md` through `ADMIN/RECOVERY.md`. Read CURRENT_SAVE and make a bounded INSTANCE/ARCHIVE inventory. If bound, or if prior-run evidence, non-template records, or unexplained files exist, use a clean separate kit. Never clear ARCHIVE. Bound upgrades use `ADMIN/UPGRADE_V07.md`.
2. Require safe ids: ASCII letters, digits, `.`, `_`, `-`, beginning with a letter or digit, without separators or traversal. Resolve only the named module and its engine. Engine id/class and flat filename stem or directory name must match; exactly one engine entrypoint may exist.
3. For LOAD, read MODULE, SETTING_BRIEF, available POLICY setup proposals, the PC entrypoint/bundle, and T0. A legacy module may lack POLICY when the accepted run proposal supplies its voice/defaults/source expectations. Follow selected capability routes needed to check advertised bodies or opening dependencies. For NEW GAME, check accepted draft equivalents; new modules include POLICY and the new module destination must still be absent.
4. Read `MODULES/_CONTRACT.md`, `ENGINE/_CONTRACT.md`, and `INSTANCE/_SCHEMA.md`. Check descriptor/routes, substantive public brief, real capability bodies, safe PC route closure, engine support/fields, and opening mechanics. Legacy descriptors/T0 may be manually mapped into a reviewed bind draft without source modification; label incomplete scripted coverage honestly. A missing or invalid Setting Brief needs a separately authorized repair before LOAD.
5. Show the complete five-section run agreement plus PC/opening facts, grants, source expectations, meaningful omissions, and operator limits. POLICY proposes defaults; loading it is not acceptance. Copy accepted voice into Presentation and accepted stronger module limits into SAFETY with their origin labeled. If voice is missing, offer the concise concrete-prose default from NEW_GAME for acceptance rather than blocking on an absent source preference. Every operative source/fidelity constraint belongs in the agreement/brief or a real source body reached by an exact contract/descriptor route, never solely in POLICY; the brief retains domain/trigger/capability wording rather than body filenames. Ordinary explicit acceptance suffices. Reuse NEW GAME's accepted proposal without another interview or confirmation.

## Shared recoverable bind

This is one operation for either entry. Follow `ADMIN/RECOVERY.md` before mutation: prepare an operation record, verified preimages for every changed existing file, the exact new-file list, and active marker. Failure or interruption leaves recovery pending; no automatic rollback is promised.

1. Recheck collisions and unchanged preconditions. For NEW GAME, write only accepted engine/module bodies and bounded private preparation whose envelope was accepted. Existing LOAD modules remain unchanged. Extend and verify the recovery record before writing any additional path.
2. Assign unique portable campaign, contract, and save ids. Build `INSTANCE/CAMPAIGN_CONTRACT.candidate.md` from the accepted agreement with matching campaign/module, `contract_rev: 1`, `contract_parent: none`, `status: candidate`, and all five sections. Never infer grants from genre, profile, defaults, or earlier model behavior.
3. Copy the exact accepted PC bundle rooted at `MODULES/<id>/CHAR/PC.md` to matching paths under `INSTANCE/CHAR/`, preserving bytes and relative paths. Do not copy unrelated siblings. Runtime PC authority is `INSTANCE/CHAR/PC.md` and its exact routed bundle.
4. Build `INSTANCE/CURRENT_SAVE.candidate.md` from accepted T0 using the v0.7 table/sections. Preserve accepted `datetime`, `place`, and all five substantive bodies. Assign run/save ids, `save_rev: 1`, `save_parent: none`, `commit_kind: bind`, `archive_ref: none`, `evidence_through: none`, and `pc_record: INSTANCE/CHAR/PC.md`. Set safety from accepted limits. Legacy missing values require explicit accepted mapping, including honest none/unspecified where appropriate.
5. Write accepted SAFETY sentences under canonical labels with module-origin labels where applicable. Initialize only canonical empty instance registers and bound-empty optional Bearing per schema. This is initialization, not REVIEW. Do not bulk-copy people, clocks, factions, or possibilities. Active processes carries relevant live conditions/non-revelatory private watch routes; Relevant records contains useful exact routes, not a compulsory roster. Until a system/person current record materializes, these save pointers use its existing MODULE baseline body. Its definition may name the later INSTANCE destination, but the save must not point to a nonexistent current file. The PC route is created by step 3.
6. Verify candidates, PC closure, source/opening agreement, limits, matching identities, current routes, and every affected file. Each detailed current value has one selected authority. MODULE mutable baselines are as-of-T0; later changes use INSTANCE. For complex capabilities, rehearse one need → route → body → stop lookup. Optional validator results establish structure, not semantic acceptance.
7. Set the verified contract to `status: accepted`, publish it, then publish CURRENT_SAVE last. Read back and verify the whole bind. Remove only named temporary candidates after verification; no recursive deletion. Complete the recovery record and remove its active marker as `ADMIN/RECOVERY.md` directs. Retain recovery materials.

If a write, readback, or semantic check fails, do not announce success or begin PLAY. Complete only the accepted operation or restore verified preimages through recovery. Publishing CURRENT_SAVE last reduces ambiguity; it is not atomicity.

## Result

Report campaign, contract/save ids, real source/mechanical limitations, and resume entrypoint `OS/AGENTS.md`. Recommend a fresh PLAY chat. Normal boot loads accepted runtime records and the Setting Brief; it does not automatically read POLICY, detailed lore, or Bearing. A bind begins with no archived PLAY evidence.
