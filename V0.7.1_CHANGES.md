# RPG OS v0.7.1 — focused repair

v0.7.1 repairs verified omissions in the v0.7 file-operated design. It keeps the existing storage areas, five agreement sections, readable current save, and one-model workspace operation. No campaign is bundled or migrated by this release.

## Agreement and runtime

- Require five short named prose clauses in their existing sections: Play form, Form selection, Structure disclosure, Cuts, and Retcon. Values remain ordinary language; A–E form labels are optional presets, not mandatory enums.
- Propose the defaults within the existing compact NEW GAME review rather than adding five interview rounds. Validate clause occurrence, placement, and substantive text while leaving semantic acceptance and compatibility to review.
- Separate accepted form, delegated form selection, and separately delegated structural disclosure. Plot secrecy does not imply opacity. The control record stays truthful about the actual form or its selection envelope.
- Preserve the operative commitments of a concealed selection through existing current/private records and non-revealing routes. A fresh chat does not reselect merely because it restarted.
- Allow knowingly accepted fixed destinations/outcomes only within their granted scope. Do not portray them as open rolls or extend them into reserved PC decisions.
- Establish lived continuity as the default and bounded hard cuts as an explicit exception, with consistent runtime/resume handling.
- Make valid-outcome rewind follow Retcon, including optional ironman/no-retcon. Genuine error repair, stop/end, and changes to depiction remain separate; an unwanted fair outcome is not automatically an error.
- Put off-premise handling into runtime instructions: use ordinary world consequences when within scope and OOC return/recalibration/end when genuinely off-contract. Do not fabricate physics or irresistible commands to enforce a premise.
- Separate external content restrictions, operator depiction limits, PC authorship, and fictional NPC conduct/world morality. Applicable host restrictions remain in force.

## Recovery and validation

- Record new directories and their verified pre-existence/creation ownership before campaign writes.
- Restore protected files and remove only recorded, operation-created, now-empty directories, deepest first, by exact nonrecursive removal inside the workspace. Preserve pre-existing directories, recovery material, and unexpected content.
- Keep the active marker until the restored files, routes, and directory structure agree. Retain orphan-directory detection.
- Fix two malformed provenance diagnostic calls with explicit named arguments. Validator mismatch or modification during execution still returns INCOMPLETE, with correctly shaped messages and line fields.
- Add regression coverage for missing/misplaced/duplicate/placeholder agreement clauses, both provenance failure paths, interrupted-save directory restoration, and protected/nonempty/out-of-scope cleanup cases.
- Add proposed behavioral fixtures for opacity, ironman, scoped cuts, and off-premise handling. These are specifications, not claimed live-play results.

## Compatibility and documentation

The v0.7 save layout remains unchanged. A bound v0.7 agreement missing the named clauses receives focused accepted supplementation through ADMIN/RECALIBRATE.md in a protected copy; that repair does not reset current state or rewrite history. Older field-only campaigns still use ADMIN/UPGRADE_V07.md. The original v0.7 ZIP remains a separate artifact.

Restored project-author attribution and explicit third-party-rights language. Clarified that the startup packet does not pin every engine, module, agreement, and mutable-record revision, and cannot detect every plausible file-version swap.

See [VERIFICATION](VERIFICATION.md) for checks actually run. This remains experimental; live campaign, fresh-chat, and cross-provider behavior need observation before relying on a migration of a real campaign.
